/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package practice;

/**
 *
 * @author Albert
 */
public class LinkedLists {
    /**
     * Insert a node into a sorted linked list (in order). (Don't forget about 
     * what happens when the new element is at the start or end!)
     */
    
    /**
     * "Sort" a linked list that contains just 0s and 1s. That is, modify the
     * list such that all 0s come before all 1s.
     */
    
    /**
     * Write code to remove duplicates from an unsorted linked list.
     */
    
    /**
     * Implement an algorithm to find the kth to last element of a singly 
     * linked list.
     */
    
    /**
     * Implement an algorithm to delete a node in the middle of a singly linked 
     * list, given only access to that node.
     */
    
    /**
     * Write code to partition a linked list around a value x, such that all 
     * nodes less than x come before all nodes greater than or equal to x. If 
     * x is contained within the list, the values of x only need to be after 
     * the elements less than x (see below).
     */
    
    /**
     * You have two numbers represented by a linked list, where each node 
     * contains a single digit. The digits are stored in reverse order, such 
     * that the 1's digit is at the head of the list. Write a function that 
     * adds the two numbers and returns the sum as a linked list.
     */
    
    /**
     * Implement a function to check if a linked list is a palindrome.
     */
    
    /**
     * Given two (singly) linked lists, determine if the two lists intersect. 
     * Return the intersecting node. Note that the intersection is defined 
     * based on reference, not value. That is, if the kth node of the first 
     * linked list is the exact same node (by reference) as the jth node of the 
     * second linked list, then they are are intersecting.
     */
    
    /**
     * Given a circular linked list, implement an algorithm that returns the 
     * node at the beginning of the loop.
     */
}
